name: Market Data & AI Disclosure Automation

on:
  schedule:
    - cron: "30,45 22 * * 0-4"
    - cron: "*/15 23 * * 0-4"
    - cron: "*/15 0-9 * * 1-5"
  workflow_dispatch:

jobs:
  automation:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      DART_API_KEY: ${{ secrets.DART_API_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 필요한 핵심 라이브러리만 설치 (pytest-cov 등 불필요한 도구 제외)
          pip install requests pandas supabase pykrx urllib3 groq

      # 🔍 아래 단계들은 에러를 유발하는 pytest/coverage 설정을 완전히 무시하고
      # 오직 실제 서비스 스크립트만 순차적으로 실행합니다.

      - name: Run Market Indices Update
        run: python scripts/update_indices.py

      - name: Run AI Disclosure Crawler
        run: python scripts/dart_crawler.py
  
      - name: Run AI Analyst
        run: python scripts/auto_analyst.py